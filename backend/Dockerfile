FROM python:3.12-slim
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1
WORKDIR /app
COPY pyproject.toml .
COPY app ./app
COPY alembic.ini .
COPY alembic ./alembic
RUN pip install --no-cache-dir .
RUN mkdir -p /app/uploads
EXPOSE 8000
CMD ["sh", "-c", "alembic upgrade head && python -m app.migrate_credentials && python -m app.bootstrap_admin && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
